services:
  jskos-proxy:
    image: ghcr.io/gbv/jskos-proxy:0.2
    environment:
      - CONFIG=examples/uri.gbv.de
      - VIRTUAL_HOST=uri.gbv.de
      - VIRTUAL_PORT=3555
      - VIRTUAL_PATH=/terminology/
    restart: unless-stopped

networks:
  default:
    external: true
    name: nginx
